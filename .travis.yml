language: java
script:
  # Test and build app
  - mvn clean verify --file ./simple-api/pom.xml

  # Connexion à docker
  - echo "$dockerPassword" | docker login -u "$dockerUserName" --password-stdin

  # Build
  - docker build -t $dockerUserName/tp-devops simple-api

  # Publication des images
  - docker push $dockerUserName/tp-devops

  # Sonarcloud
  - mvn clean verify org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar -Dsonar.projectKey=elenaortega_correction-tp1

  addons:
    sonarcloud:
      oganization: "elenaortega"
        token: "20c33928cd48f8bf50540752da1dbd1d86e51933"

on:
  branch: develop

cache:
  directories:
    - $HOME/.m2

services:
  - docker
